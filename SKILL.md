---
name: wiki-builder
description: 为项目、角色或学习线搭建、升级和治理工作型 wiki。适用于新建 wiki、升级旧 wiki、wiki ingest、wiki lint、多 wiki 入口设计。默认采用 `CLAUDE.md + raw/ + notes/ + outputs/ + state/ + wiki/` 的工作型 wiki 骨架，并通过 `wiki_map.md` 处理多个 wiki 并存时的总导航。
---

# Wiki Builder（分享版）

## 核心口径

- **只负责 wiki 的搭建、升级、ingest 与治理**
- **不讨论 wiki 之外的其他仓位或执行体系**
- **工作型 wiki 的默认运行骨架**，不再只停留在 `CLAUDE.md + raw/ + outputs/ + wiki/`
- **多个 wiki 并存时，可通过 `wiki_map.md` 处理总导航**
- **长期判断归 `wiki`，本地当前态归 `state/`，中间知识层归 `notes/`**

---

## 一句话总原则

**`wiki-builder` 只负责把 wiki 建成“能持续运行”的知识系统：有本地 schema、有任务入口、有当前运行态、有正式知识层。**

---

## 第一步：建 wiki 时，默认用这套运行骨架

### 基础兼容骨架

旧版最小骨架仍可兼容：

```text
CLAUDE.md + raw/ + outputs/ + wiki/
```

### 当前推荐骨架（工作型 wiki 默认）

```text
{wiki目录}/
├── CLAUDE.md                      ← 本地 schema：边界 / 启动链 / 升级门槛
├── raw/                           ← 原始资料层：只读不写
│   └── README.md
├── notes/                         ← 中间知识层：原子笔记 / 判断卡 / 方法卡 / 边界卡
│   └── README.md
├── outputs/                       ← 阶段产出层
│   ├── README.md
│   ├── 实践记录/
│   ├── 待验证洞察/
│   └── 问答存档/
├── state/                         ← 当前运行态：阶段 / 优先级 / 约束 / 信号 / 队列
│   └── 00_*.md
├── wiki/                          ← 稳定知识层
│   ├── index.md
│   ├── log.md
│   ├── moc/                       ← 任务地图 / 路由页
│   └── ...正式方法页 / SOP / 判断标准
└── schema/                        ← 可选：仅做旧入口兼容
    └── WIKI_RULES.md
```

### 为什么要补 `notes/`、`state/`、`wiki/moc/`

因为只靠 `raw + outputs + wiki`，会很快遇到 3 个问题：

1. **中间知识没地方放**：判断卡、边界卡、原子笔记容易误塞进 `wiki/`
2. **当前运行态没地方放**：阶段策略、当前优先级、最近信号被写成长期规则
3. **任务入口太绕**：只靠 `index.md` 很难承接复杂任务流，必须补 `wiki/moc/`

---

## 第二步：明确各层职责

### `raw/`

放原始资料：下载原表、逐字稿、OCR、PDF 转 Markdown、外部文章摘录、截图整理。

规则：
- 只读不写
- 不直接在这里写结论
- 新资料默认先放这里

### `notes/`

放**中间知识层**，不是草稿垃圾箱：
- 原子笔记
- 判断卡
- 方法卡
- 反例卡
- 协商后的边界说明
- 对话结晶

规则：
- 能拆成最小知识单元的内容，优先先落这里
- 它既不是正式 wiki，也不是一次性对话堆放区
- 后续通过双链连接到 `wiki/`、`state/`、`raw/`、`outputs/`

### `outputs/`

放**阶段产出层**：
- `实践记录/`：真实做过的动作、复盘、执行结果
- `待验证洞察/`：新判断、新策略、新口径，但还没跑成熟
- `问答存档/`：一次性分析、对比、诊断、方案稿

### `state/`

放**当前运行态**：
- 当前阶段与目标
- 当前任务池 / 优先级
- 当前约束与禁止事项
- 最近 7-14 天有效信号
- ingest 队列 / 待升级队列

规则：
- `state/` 只表达“现在怎么跑”
- 进入具体任务前，默认优先先读 `state/`
- 阶段变化时优先更新这里，不要先改长期 SOP

### `wiki/`

只放稳定知识：
- SOP
- 判断标准
- 长期规则与边界
- 多次验证的方法
- 已被明确确认、后续默认按此执行的内容

### `wiki/moc/`

只做**任务地图与路由**，不做大而全正文。它负责回答：
- 这次任务属于哪类工作流
- 应该先读哪些文件
- 需要补读哪份 `state/`
- 本轮产出完成后应回写哪一层

---

## 第三步：多个 wiki 并存时的启动链

### 总启动链

```text
1. 先读工作区根目录 wiki_map.md
2. 判断这次任务属于哪个 wiki
3. 进入目标 wiki 后，先读它自己的 CLAUDE.md
4. 再读 wiki/index.md 或本地入口页
5. 再进入 wiki/moc/ 任务地图
6. 再读对应 state/
7. 最后精准读正式页 / SOP / 原始资料
8. 读完之前，不直接凭印象开工
```

### 本地 wiki 内的最小启动链

```text
CLAUDE.md → wiki/index.md → wiki/moc/ → state/ → 正式页 / SOP → raw|notes|outputs（按需补读）
```

### 强规则

- 不能跳过 `wiki_map.md` 做多 wiki 导航
- 不能跳过 `CLAUDE.md` 直接读正文页
- 不能只读一个正式页就假装读过整个 wiki
- 不能把旧记忆当作当前阶段唯一权威

---

## 第四步：内容落位规则

| 情况 | 去哪 |
|---|---|
| 新拿到的原始资料 | `raw/` |
| 可拆成最小知识单元的判断 / 方法 / 反例 / 边界说明 | `notes/` |
| 真实发生的实践与复盘 | `outputs/实践记录/` |
| 新想法，但还没验证 | `outputs/待验证洞察/` |
| 这轮问答形成了一份分析或方案 | `outputs/问答存档/` |
| 当前阶段 / 当前优先级 / 当前约束 / 最近信号 | `state/` |
| 已稳定、可复用、边界清晰的方法与规则 | `wiki/` |

### 一句话判断

- **还没验证 / 只发生一次 / 只是这轮回答** → 先放 `notes/` 或 `outputs/`
- **当前正在指导执行，但还不是长期方法** → 放 `state/`
- **已经稳定 / 可复用 / 被确认以后按这个来** → 才进 `wiki/`

### `notes/` 和 `outputs/` 怎么分

- **`notes/`**：更像中间知识层、原子层、关系层
- **`outputs/`**：更像阶段产出层、执行结果层、问答沉淀层

简单说：
- 能拆成“最小判断卡”的，优先进 `notes/`
- 已形成“这轮交付结果”的，优先进 `outputs/`

---

## 第五步：Ingest 默认工作流

```text
新资料进入 raw/
    ↓
先拆成 notes/ 原子卡 / 判断卡 / 边界卡
    ↓
再判断该去 outputs / state / wiki
    ↓
如新增任务入口或知识入口，同步更新 wiki/index.md / wiki/moc/
    ↓
在 wiki/log.md 记录本次变更
```

### 重要规则

- 不要因为内容有价值，就直接写进 `wiki/`
- 先问：它是稳定规则、当前运行态，还是过程产物
- 进入 wiki 的，不一定是“来源本身”，很多时候是消化后的产物

---

## 第六步：从 `notes/` / `outputs/` 升级到 `wiki/` 的门槛

以下 4 条至少满足前 3 条，且第 4 条必须成立：

1. **来源清楚**：知道来自哪份资料、哪次讨论、哪次验证
2. **结论稳定**：不是一句临时灵感或单轮聊天判断
3. **边界明确**：知道适用于什么场景，不适用于什么场景
4. **获得确认**：要么用户明确确认，要么已经被真实实践验证

### 升级要求

- 先整理成短、稳、可执行条目
- 不把聊天原话或过程稿直接搬进 `wiki/`
- 如与旧规则冲突，先改旧规则，不做重复叠加
- 项目知识先更新项目层，不直接跳写全局记忆

---

## 第七步：升级旧 wiki 时，默认怎么改

### 场景 1：只有 `raw + wiki + CLAUDE.md`

默认补：
1. `outputs/`
2. `notes/`
3. `state/`
4. `wiki/moc/`
5. 更新 `CLAUDE.md`
6. 更新 `wiki/index.md`
7. `wiki/log.md` 记录结构升级

### 场景 2：已经有 `raw + outputs + wiki`

重点补：
- `notes/` 解决中间知识落位
- `state/` 解决当前口径来源
- `wiki/moc/` 解决任务入口
- 如工作区里有多个 wiki，再补 `wiki_map.md` 做总导航

### 场景 3：已经有很多页面，但越来越乱

先做 `wiki lint`，而不是继续加页：
- 查重复页
- 查冲突口径
- 查是否把阶段性内容误写进正式页
- 查是否缺 `state/`
- 查是否缺 `MOC`
- 查是否已有多个 wiki 却没维护 `wiki_map.md`

### 升级目标

解决 5 类老问题：
- 一次性问答没地方放
- 中间判断没地方放
- 当前阶段判断误进长期 SOP
- 任务入口越来越绕
- 多个 wiki 并存时经常走错入口

---

## 第八步：`wiki_map.md` 的职责

当工作区里有多个 wiki 时，`wiki_map.md` 只负责回答一件事：

**这次任务该先进哪个 wiki。**

它不负责：
- 替代各 wiki 的 `CLAUDE.md`
- 写各 wiki 的正式 SOP
- 充当大而全的知识正文页

### 至少要写清的字段

| Wiki | 服务范围 | 何时优先进入 | 主入口 |
|---|---|---|---|
| `运营wiki` | 某一业务线的长期判断与 SOP | 任务属于该业务线时 | `CLAUDE.md` → `wiki/index.md` |
| `研究wiki` | 某类方法、知识迁移、框架沉淀 | 任务重点是方法总结与迁移时 | `CLAUDE.md` → `wiki/index.md` |
| `教学wiki` | 备课、教学设计、课堂结构 | 任务重点是课程或教学设计时 | `CLAUDE.md` → `wiki/index.md` |

---

## 第九步：`wiki lint` 最小检查项

当用户说“还是不好用”“结构还是绕”“知识还是不独立”，或新补了一批页面后，默认做一轮 `wiki lint`：

1. **入口检查**：`CLAUDE.md` / `wiki/index.md` / `wiki/moc/` / `state/` / `wiki/` 是否仍各司其职
2. **导航检查**：多个 wiki 并存时，`wiki_map.md` 是否还能把任务导到对的 wiki
3. **状态检查**：阶段性判断是否回收到了 `state/`
4. **升级检查**：本该进 `notes/` / `outputs/` / `state/` 的内容，是否误进了 `wiki/`
5. **冲突检查**：同一问题是否同时存在旧口径和新口径
6. **孤岛页检查**：新页能不能回到 `index` / `MOC` / 上位页

### 处理原则

- **重复或冲突**：优先合并、替换、降级，不并存
- **阶段性判断**：优先回收到 `state/`
- **一次性分析**：优先回收到 `outputs/`
- **只够局部复用的知识**：优先留在 `notes/`

---

## 最小模板要求

### `CLAUDE.md` 至少写清

- 服务范围 / 不服务什么
- 多 wiki 并存时的启动链
- 本地目录分工
- ingest 工作流
- 升级门槛

### `wiki/index.md` 至少写清

- 这个 wiki 解决什么问题
- 先读哪些任务地图 / 正式页
- 当前 `state/` 入口是什么
- `notes/` 和 `outputs/` 怎么分
- 常见任务该先读哪几个文件

### `wiki/moc/` 至少做到

- 回答“这次任务该先走哪条工作流”
- 指向对应 `state/` 与正式页
- 说明每条工作流的起手顺序

### `state/` 至少做到

- 表达当前怎么跑
- 不替代正式方法页
- 阶段变化时优先更新这里

### `notes/README.md` 至少写清

- 什么内容适合先落 `notes/`
- 什么内容不该直接升 `wiki/`
- 原子笔记和正式页之间如何连

---

## 推荐示例

- `运营wiki`：承接某条业务线的 SOP、复盘与数据判断
- `研究wiki`：承接方法论、框架和跨场景迁移
- `教学wiki`：承接备课、教学流程和课堂设计

---

## 最后的默认判断

- **先定 wiki 入口，再补正文**
- **先把当前态与中间层放对，再升级正式页**
- **只要建 wiki，就默认把它建成“能持续运行”的 wiki，而不是只有几张好看的页面**
